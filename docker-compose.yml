services:
  guildsigil095:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3000
    ports:
      - "${SERVER_PORT:-3095}:3000"
    restart: unless-stopped
